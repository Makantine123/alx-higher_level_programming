#!/usr/bin/python3

def divisible_by_2(my_list=[]):
    j = 0
    for i in range(len(my_list)):
        if my_list[i] % 2 == 0:
            new_list[j] = my_list[i]
            j += j
    return new_list
