#!/usr/bin/python3

def multiple_returns(sentence):
    if sentence = "":
        strlen = 0
        firstChar = None
    else:
        strlen = len(sentence)
        firstChar = sentence[0]
    
    tupleR = (strlen, firstChar)
    return tupleR
