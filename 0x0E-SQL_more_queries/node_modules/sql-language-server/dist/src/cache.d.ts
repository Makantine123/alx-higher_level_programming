import { Diagnostic as SQLintDiagnostic } from 'sqlint';
import { Diagnostic, Range } from 'vscode-languageserver';
export declare type LintCache = {
    lint: SQLintDiagnostic;
    diagnostic: Diagnostic;
};
declare class Cache {
    private _lintResult;
    setLintCache(uri: string, lintCache: LintCache[]): void;
    getLintCache(uri: string): LintCache[];
    findLintCacheByRange(uri: string, range: Range): LintCache | null;
}
declare const _default: Cache;
export default _default;
