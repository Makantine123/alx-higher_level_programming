import { FromTableNode } from '@joe-re/sql-parser';
import { Table } from '../../database_libs/AbstractClient';
export declare function createSelectAllColumnsCandidates(fromNodes: FromTableNode[], tables: Table[], lastToken: string): {
    label: string;
    insertText: string;
    filterText: string;
    detail: string;
    kind: 23;
}[];
