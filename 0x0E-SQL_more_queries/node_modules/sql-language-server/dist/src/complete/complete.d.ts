import { CompletionItem } from 'vscode-languageserver-types';
import { Schema } from '../database_libs/AbstractClient';
export declare type Pos = {
    line: number;
    column: number;
};
declare type CompletionError = {
    label: string;
    detail: string;
    line: number;
    offset: number;
};
export declare function complete(sql: string, pos: Pos, schema?: Schema, jupyterLabMode?: boolean): {
    candidates: CompletionItem[];
    error: CompletionError | null;
};
export {};
